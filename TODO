get key from child?
allow centering current slide in middle
add responsive height
clean up Swiper and write tests
slides of different sizes? what controls swipe dimensions? first slide?
compare with react-slick, swiper-js, keen-slider (similar implementation), nuka-carousel
other props:autoplay, spacing, lazy loading
different easing functions
handle different slide widths (only when !lazy)
slides of different sizes? change visible to fitted and make optional
change onSwipeStart to include direction and change in direction
